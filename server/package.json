{
  "name": "server",
  "module": "src/server.ts",
  "main": "dist/server.js",
  "scripts": {
    "dev": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^20.12.12",
    "nodemon": "^3.1.0"
  },
  "peerDependencies": {
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "socket.io": "^4.7.5"
  }
}